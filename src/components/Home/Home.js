import React from 'react';

const Home = () => {
  return (
        <div>
          <div className='home-bg'>
           <button type='button' className='btn btn-primary mt-5'>Search for places</button>
           </div>
        </div>
  );
};

export default Home;
